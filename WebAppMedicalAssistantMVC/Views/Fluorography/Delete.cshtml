@model FluorographyModel

@{
    ViewData["Title"] = "Удаление флюорографии";
    Layout = "_Layout";
}
<div>
    <h4>Вы уверены, что хотите удалить флюорографию?</h4>
    <p>@Model.DataOfExamination</p>
    <p>@Model.NumberFluorography</p>
</div>
<form asp-controller="Fluorography" asp-action="Delete" method="post" asp-route-id="@Model.Id">
    <div>
        <input type="submit" class="btn btn-danger m-1" value="Удалить флюорографию" />
        <a class="btn btn-primary m-1" role="button" asp-controller="Fluorography" asp-action="Index">Отмена</a>
    </div>
</form>

@section Scripts
    {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
}