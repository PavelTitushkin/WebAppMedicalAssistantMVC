@model IEnumerable<WebAppMedicalAssistant_Core.DTO.MedicineDto>

@{
    ViewData["Title"] = "Админ";
    Layout = "_AdminLayout";
}

<h2>Лекарства</h2>

<p>
    <a asp-action="AddMedicine">Добавить новое лекарство</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Название
            </th>
            <th>
                Инструкция
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NameOfMedicine)
                </td>
                <td>
                    @if(!string.IsNullOrEmpty(item.LinkToInstructions))
                    {
                        <a class="m-1" href="@item.LinkToInstructions">Инструкция</a>
                    }
                    else
                    {
                        <p class="m-1">Инструкция отсутсвует</p>
                    }
                </td>
                <td>
                    @Html.ActionLink("Редактировать", "ShowEditMedicine", "Admin", new {id = item.Id, nameMedicine = item.NameOfMedicine, linkToInstructions = item.LinkToInstructions})
                    <a asp-controller="Admin" asp-action="ShowDeleteMedicine" asp-route-id="@item.Id" asp-route-nameOfMedicine="@item.NameOfMedicine" class="m-1">Удалить</a>
                </td>
            </tr>
        }
    </tbody>
</table>
